# RAG Chat Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Thêm tính năng hỏi AI về nội dung tài liệu (RAG chat) vào trang chi tiết tài liệu, hỗ trợ PDF, DOCX và PPTX.

**Architecture:** Naive RAG — `contentText` đã lưu sẵn trong MongoDB, chunk in-memory (400 ký tự, overlap 50), embed câu hỏi + chunks bằng `text-embedding-3-small`, cosine similarity chọn top-4 chunks, gửi lên `gpt-4o-mini`. Chunk embeddings cache trong `Map` theo `materialId` để tránh re-embed khi cùng tài liệu được hỏi nhiều lần. PPTX extraction đã được implement trong `extractText.js` — chỉ cần re-embed các file PPTX cũ chưa có `contentText`.

**Tech Stack:** Node.js/Express, Next.js (App Router), OpenAI SDK, MongoDB/Mongoose, officeparser (đã có)

---

## File Map

| File | Vai trò |
|---|---|
| `Backend/utils/extractText.js` | Đã có PPTX support — không sửa |
| `Backend/services/aiService.js` | Thêm `chunkText`, `cosineSimilarity`, `chatWithDocument`, `chunkCache` |
| `Backend/controllers/materialController.js` | Fix `getMaterialById`: exclude `contentText`/`embedding`, thêm `hasContentText` + fix `avatar` populate |
| `Backend/controllers/recommendationController.js` | Thêm `chatDocument` handler |
| `Backend/routes/recommendationRoutes.js` | Thêm `POST /chat` với `aiLimiter` |
| `frontend/src/components/DocumentChat.js` | Component chat mới |
| `frontend/src/app/documents/[id]/page.js` | Nhúng `<DocumentChat>` vào cuối left column |

---

## Task 1: Xác nhận PPTX support và re-embed file cũ

**Files:**
- Verify: `Backend/utils/extractText.js`
- Verify: `Backend/package.json`
- Run: `Backend/scripts/generate_embeddings.js`

- [ ] **Step 1: Kiểm tra officeparser đã được cài chưa**

```powershell
cd Backend
cat package.json | Select-String "officeparser"
```

Nếu chưa có:
```powershell
npm install officeparser --legacy-peer-deps
```

- [ ] **Step 2: Xác nhận extractText.js đã xử lý pptx**

Mở `Backend/utils/extractText.js` và kiểm tra:
- `require("officeparser")` ở đầu file
- Case `type === "pptx"` gọi `officeParser.parseOfficeAsync(buffer)` trong `extractText()`

Nếu thiếu, thêm vào `extractText()` sau case `docx`:
```javascript
if (type === "pptx") {
  const text = await officeParser.parseOfficeAsync(buffer);
  return normalize(text);
}
```

- [ ] **Step 3: Re-embed các PPTX cũ trong DB**

```powershell
cd Backend
node scripts/generate_embeddings.js
```

Script này đọc tất cả tài liệu trong DB có `embedding` rỗng hoặc thiếu, trích lại text và tạo embedding. Kiểm tra output để đảm bảo không có lỗi.

- [ ] **Step 4: Commit**

```powershell
git add Backend/package.json
git commit -m "feat: ensure officeparser installed for PPTX text extraction"
```

(Nếu package.json không thay đổi thì skip commit này.)

---

## Task 2: Thêm RAG functions vào aiService.js

**Files:**
- Modify: `Backend/services/aiService.js`

- [ ] **Step 1: Thêm `chunkText` và `cosineSimilarity`**

Mở `Backend/services/aiService.js`. Sau dòng `const openai = new OpenAI(...)`, thêm:

```javascript
// Cắt text thành các chunk ~chunkSize ký tự, overlap ký tự giữa 2 chunk liền kề.
const chunkText = (text, chunkSize = 400, overlap = 50) => {
  const chunks = [];
  let i = 0;
  while (i < text.length) {
    chunks.push(text.slice(i, i + chunkSize));
    i += chunkSize - overlap;
  }
  return chunks.filter((c) => c.trim().length > 0);
};

const cosineSimilarity = (vecA, vecB) => {
  let dot = 0, normA = 0, normB = 0;
  for (let i = 0; i < vecA.length; i++) {
    dot += vecA[i] * vecB[i];
    normA += vecA[i] * vecA[i];
    normB += vecB[i] * vecB[i];
  }
  if (normA === 0 || normB === 0) return 0;
  return dot / (Math.sqrt(normA) * Math.sqrt(normB));
};

// Cache chunk embeddings theo materialId để tránh re-embed khi cùng tài liệu được hỏi nhiều lần.
// Mất khi server restart — đây là behavior đúng vì contentText có thể thay đổi.
const chunkCache = new Map();
```

- [ ] **Step 2: Thêm `chatWithDocument`**

Ở cuối file `aiService.js`, trước dòng `module.exports`, thêm:

```javascript
const chatWithDocument = async (materialId, question) => {
  const Material = require("../models/Material");

  const doc = await Material.findById(materialId).select("contentText title");
  if (!doc) {
    const err = new Error("Tài liệu không tồn tại");
    err.statusCode = 404;
    throw err;
  }
  if (!doc.contentText || doc.contentText.trim().length === 0) {
    const err = new Error("Tài liệu này chưa hỗ trợ chat");
    err.statusCode = 400;
    throw err;
  }

  const cacheKey = materialId.toString();
  if (!chunkCache.has(cacheKey)) {
    const chunks = chunkText(doc.contentText);
    const vectors = await Promise.all(chunks.map((chunk) => generateEmbedding(chunk)));
    chunkCache.set(
      cacheKey,
      chunks.map((chunk, i) => ({ chunk, vector: vectors[i] }))
    );
  }
  const cached = chunkCache.get(cacheKey);

  const questionVector = await generateEmbedding(question);

  const top4 = cached
    .map(({ chunk, vector }) => ({ chunk, score: cosineSimilarity(questionVector, vector) }))
    .sort((a, b) => b.score - a.score)
    .slice(0, 4);

  const contextText = top4.map((s) => s.chunk).join("\n---\n");

  const response = await openai.chat.completions.create({
    model: "gpt-4o-mini",
    messages: [
      {
        role: "system",
        content:
          "Bạn là trợ lý học tập. Dựa vào các đoạn trích dưới đây từ tài liệu, hãy trả lời câu hỏi một cách chính xác và súc tích bằng tiếng Việt. Nếu thông tin không có trong các đoạn trích, hãy nói rõ là không tìm thấy trong tài liệu.",
      },
      {
        role: "user",
        content: `Context:\n${contextText}\n\nCâu hỏi: ${question}`,
      },
    ],
    max_tokens: 500,
    temperature: 0.3,
  });

  return { answer: response.choices[0].message.content.trim() };
};
```

- [ ] **Step 3: Export `chatWithDocument`**

Tìm dòng `module.exports` cuối file và thêm `chatWithDocument`:

```javascript
module.exports = { generateEmbedding, generateMetadata, expandQuery, buildEmbeddingText, chatWithDocument };
```

- [ ] **Step 4: Test thủ công**

Tạo file tạm `Backend/scripts/test_rag.js`:

```javascript
require("dotenv").config();
const connectDB = require("../config/db");
const { chatWithDocument } = require("../services/aiService");

(async () => {
  await connectDB();
  // Thay ID bằng 1 tài liệu PDF/DOCX có contentText trong DB của bạn
  const materialId = "PASTE_A_REAL_MATERIAL_ID_HERE";
  const result = await chatWithDocument(materialId, "Tài liệu này nói về chủ đề gì?");
  console.log("Answer:", result.answer);
  process.exit(0);
})();
```

Chạy:
```powershell
node scripts/test_rag.js
```

Expected: in ra câu trả lời từ GPT dựa trên nội dung tài liệu (không phải lỗi).

- [ ] **Step 5: Xóa file test tạm**

```powershell
Remove-Item Backend/scripts/test_rag.js
```

- [ ] **Step 6: Commit**

```powershell
git add Backend/services/aiService.js
git commit -m "feat: add chunkText, cosineSimilarity, chatWithDocument to aiService"
```

---

## Task 3: Fix getMaterialById — loại bỏ trường nặng, thêm hasContentText

**Files:**
- Modify: `Backend/controllers/materialController.js` (hàm `getMaterialById`, khoảng dòng 376–404)

- [ ] **Step 1: Thay thế hàm getMaterialById**

Tìm và thay toàn bộ hàm `getMaterialById` (từ `const getMaterialById = async` đến dấu `};` đóng hàm):

```javascript
const getMaterialById = async (req, res) => {
  try {
    // Exclude contentText (tối đa 8000 chars) và embedding (1536 floats) khỏi response.
    // Frontend chỉ cần hasContentText (boolean) để quyết định có hiện chat panel không.
    const material = await Material.findById(req.params.id)
      .select("-embedding")
      .populate("uploaderId", "fullName avatar")
      .populate("categoryId", "name")
      .populate("majorId", "name")
      .populate("tags", "name");

    if (!material) {
      return res.status(404).json({ message: "Không tìm thấy" });
    }

    material.metrics.viewCount += 1;
    await material.save();

    recordInteraction(req.user?._id, material._id, "view", 1);

    const docObj = material.toObject();
    docObj.hasContentText = !!(docObj.contentText && docObj.contentText.trim().length > 0);
    delete docObj.contentText; // không gửi full text về client

    return res.status(200).json(docObj);
  } catch (error) {
    return res.status(500).json({ message: "Lỗi server", error: error.message });
  }
};
```

- [ ] **Step 2: Kiểm tra không có chỗ nào khác trong frontend dùng `doc.contentText`**

```powershell
grep -r "contentText" frontend/src --include="*.js" --include="*.jsx" --include="*.tsx"
```

Expected: không có kết quả (frontend không dùng trực tiếp contentText).

- [ ] **Step 3: Restart backend và test**

```powershell
# Tắt backend đang chạy nếu có, rồi chạy lại
cd Backend
node server.js
```

Trong terminal khác:
```powershell
# Thay ID bằng material ID thật
curl http://localhost:5000/api/materials/MATERIAL_ID | python -m json.tool | Select-String "contentText|hasContentText|embedding"
```

Expected: không thấy `contentText` hay `embedding`, nhưng thấy `"hasContentText": true` (hoặc `false`).

- [ ] **Step 4: Commit**

```powershell
git add Backend/controllers/materialController.js
git commit -m "fix: getMaterialById excludes contentText/embedding, adds hasContentText boolean"
```

---

## Task 4: Thêm chatDocument controller

**Files:**
- Modify: `Backend/controllers/recommendationController.js`

- [ ] **Step 1: Thêm import chatWithDocument ở đầu file**

Tìm dòng đầu tiên của file:
```javascript
const recommendationService = require("../services/recommendationService");
```

Thêm ngay bên dưới:
```javascript
const { chatWithDocument } = require("../services/aiService");
const mongoose = require("mongoose");
```

- [ ] **Step 2: Thêm handler chatDocument ở cuối file**

Ngay trước dòng cuối cùng (nếu có `module.exports`) hoặc sau handler cuối cùng, thêm:

```javascript
// Hỏi AI về nội dung của một tài liệu cụ thể (RAG)
exports.chatDocument = async (req, res) => {
  try {
    const { materialId, question } = req.body;

    if (!materialId || !question?.trim()) {
      return res.status(400).json({ success: false, message: "Thiếu materialId hoặc question" });
    }
    if (!mongoose.Types.ObjectId.isValid(materialId)) {
      return res.status(400).json({ success: false, message: "materialId không hợp lệ" });
    }

    const result = await chatWithDocument(materialId, question.trim());
    res.json({ success: true, answer: result.answer });
  } catch (error) {
    const status = error.statusCode || 500;
    const message = status === 500 ? "AI đang bận, vui lòng thử lại sau" : error.message;
    res.status(status).json({ success: false, message });
  }
};
```

- [ ] **Step 3: Commit**

```powershell
git add Backend/controllers/recommendationController.js
git commit -m "feat: add chatDocument controller for RAG chat endpoint"
```

---

## Task 5: Thêm route POST /chat

**Files:**
- Modify: `Backend/routes/recommendationRoutes.js`

- [ ] **Step 1: Thêm route**

Mở `Backend/routes/recommendationRoutes.js`. Tìm dòng:
```javascript
router.get("/for-you", aiLimiter, authMiddleware, recommendationController.getForYou);
```

Thêm ngay bên dưới:
```javascript
// RAG chat — hỏi AI về nội dung 1 tài liệu cụ thể
router.post("/chat", aiLimiter, recommendationController.chatDocument);
```

- [ ] **Step 2: Test endpoint bằng curl**

Đảm bảo backend đang chạy. Thay MATERIAL_ID bằng ID thật có contentText:

```powershell
curl -X POST http://localhost:5000/api/recommendations/chat `
  -H "Content-Type: application/json" `
  -d '{"materialId":"MATERIAL_ID","question":"Tài liệu này nói về chủ đề gì?"}'
```

Expected:
```json
{"success": true, "answer": "...câu trả lời từ GPT..."}
```

- [ ] **Step 3: Test case không có contentText**

Thử với tài liệu video hoặc tài liệu không có contentText:
```powershell
curl -X POST http://localhost:5000/api/recommendations/chat `
  -H "Content-Type: application/json" `
  -d '{"materialId":"VIDEO_OR_EMPTY_ID","question":"test"}'
```

Expected: `{"success": false, "message": "Tài liệu này chưa hỗ trợ chat"}` với status 400.

- [ ] **Step 4: Commit**

```powershell
git add Backend/routes/recommendationRoutes.js
git commit -m "feat: add POST /api/recommendations/chat route with rate limiting"
```

---

## Task 6: Tạo DocumentChat component

**Files:**
- Create: `frontend/src/components/DocumentChat.js`

- [ ] **Step 1: Tạo component**

Tạo file `frontend/src/components/DocumentChat.js` với nội dung:

```javascript
"use client";
import { useState, useEffect, useRef } from "react";

export default function DocumentChat({ materialId, hasContent }) {
  const [messages, setMessages] = useState([]);
  const [input, setInput] = useState("");
  const [isLoading, setIsLoading] = useState(false);
  const bottomRef = useRef(null);

  useEffect(() => {
    bottomRef.current?.scrollIntoView({ behavior: "smooth" });
  }, [messages, isLoading]);

  if (!hasContent) return null;

  const handleSend = async () => {
    const question = input.trim();
    if (!question || isLoading) return;

    setMessages((prev) => [...prev, { role: "user", content: question }].slice(-40));
    setInput("");
    setIsLoading(true);

    try {
      const res = await fetch(
        `${process.env.NEXT_PUBLIC_API_URL || "http://localhost:5000"}/api/recommendations/chat`,
        {
          method: "POST",
          headers: { "Content-Type": "application/json" },
          body: JSON.stringify({ materialId, question }),
        }
      );
      const data = await res.json();
      if (!res.ok) throw new Error(data.message || "Lỗi không xác định");
      setMessages((prev) =>
        [...prev, { role: "assistant", content: data.answer }].slice(-40)
      );
    } catch (err) {
      const msg =
        err.message?.includes("nhiều") || err.message?.includes("nhanh")
          ? "Bạn hỏi quá nhanh, chờ chút nhé."
          : "AI đang bận, vui lòng thử lại sau.";
      setMessages((prev) => [...prev, { role: "assistant", content: msg }].slice(-40));
    } finally {
      setIsLoading(false);
    }
  };

  const handleKeyDown = (e) => {
    if (e.key === "Enter" && !e.shiftKey) {
      e.preventDefault();
      handleSend();
    }
  };

  return (
    <div className="bg-white rounded-3xl border border-slate-100 p-6 mt-8">
      {/* Header */}
      <div className="flex items-center gap-3 mb-5">
        <div className="w-8 h-8 rounded-xl bg-emerald-500 flex items-center justify-center shrink-0">
          <svg width="15" height="15" fill="none" stroke="white" viewBox="0 0 24 24" strokeWidth="2.5">
            <path strokeLinecap="round" strokeLinejoin="round" d="m12 3-1.912 5.813a2 2 0 0 1-1.275 1.275L3 12l5.813 1.912a2 2 0 0 1 1.275 1.275L12 21l1.912-5.813a2 2 0 0 1 1.275-1.275L21 12l-5.813-1.912a2 2 0 0 1-1.275-1.275L12 3Z" />
          </svg>
        </div>
        <div>
          <p className="text-sm font-black text-slate-800">Hỏi AI về tài liệu này</p>
          <p className="text-[10px] font-bold text-slate-400 uppercase tracking-widest">
            Trợ lý học tập thông minh
          </p>
        </div>
      </div>

      {/* Message list */}
      {(messages.length > 0 || isLoading) && (
        <div className="space-y-3 mb-4 max-h-96 overflow-y-auto pr-1">
          {messages.map((msg, i) => (
            <div
              key={i}
              className={`flex gap-2.5 ${msg.role === "user" ? "justify-end" : "justify-start"}`}
            >
              {msg.role === "assistant" && (
                <div className="w-7 h-7 rounded-xl bg-emerald-100 flex items-center justify-center shrink-0 mt-0.5">
                  <svg width="11" height="11" fill="none" stroke="#10b981" viewBox="0 0 24 24" strokeWidth="2.5">
                    <path strokeLinecap="round" strokeLinejoin="round" d="m12 3-1.912 5.813a2 2 0 0 1-1.275 1.275L3 12l5.813 1.912a2 2 0 0 1 1.275 1.275L12 21l1.912-5.813a2 2 0 0 1 1.275-1.275L21 12l-5.813-1.912a2 2 0 0 1-1.275-1.275L12 3Z" />
                  </svg>
                </div>
              )}
              <div
                className={`max-w-[85%] px-4 py-3 rounded-2xl text-sm font-medium leading-relaxed whitespace-pre-wrap ${
                  msg.role === "user"
                    ? "bg-slate-900 text-white rounded-tr-none"
                    : "bg-slate-50 text-slate-700 rounded-tl-none border border-slate-100"
                }`}
              >
                {msg.content}
              </div>
            </div>
          ))}

          {/* Loading dots */}
          {isLoading && (
            <div className="flex gap-2.5 justify-start">
              <div className="w-7 h-7 rounded-xl bg-emerald-100 flex items-center justify-center shrink-0">
                <svg width="11" height="11" fill="none" stroke="#10b981" viewBox="0 0 24 24" strokeWidth="2.5">
                  <path strokeLinecap="round" strokeLinejoin="round" d="m12 3-1.912 5.813a2 2 0 0 1-1.275 1.275L3 12l5.813 1.912a2 2 0 0 1 1.275 1.275L12 21l1.912-5.813a2 2 0 0 1 1.275-1.275L21 12l-5.813-1.912a2 2 0 0 1-1.275-1.275L12 3Z" />
                </svg>
              </div>
              <div className="bg-slate-50 border border-slate-100 rounded-2xl rounded-tl-none px-4 py-3.5 flex items-center gap-1">
                <span className="w-1.5 h-1.5 bg-emerald-400 rounded-full animate-bounce [animation-delay:0ms]" />
                <span className="w-1.5 h-1.5 bg-emerald-400 rounded-full animate-bounce [animation-delay:150ms]" />
                <span className="w-1.5 h-1.5 bg-emerald-400 rounded-full animate-bounce [animation-delay:300ms]" />
              </div>
            </div>
          )}

          <div ref={bottomRef} />
        </div>
      )}

      {/* Input row */}
      <div className="flex gap-2.5 items-end">
        <textarea
          value={input}
          onChange={(e) => setInput(e.target.value)}
          onKeyDown={handleKeyDown}
          placeholder="Hỏi về nội dung tài liệu... (Enter để gửi)"
          disabled={isLoading}
          rows={1}
          className="flex-1 bg-slate-50 border border-slate-200 rounded-2xl px-4 py-3 text-sm font-medium text-slate-700 outline-none focus:border-emerald-400 focus:ring-4 focus:ring-emerald-500/5 focus:bg-white transition-all resize-none placeholder:text-slate-300 disabled:opacity-50"
        />
        <button
          onClick={handleSend}
          disabled={!input.trim() || isLoading}
          aria-label="Gửi câu hỏi"
          className="shrink-0 w-11 h-11 rounded-2xl bg-emerald-500 hover:bg-emerald-400 text-white flex items-center justify-center transition-all active:scale-95 disabled:opacity-40 disabled:cursor-not-allowed"
        >
          {isLoading ? (
            <span className="w-4 h-4 border-2 border-white/40 border-t-white rounded-full animate-spin" />
          ) : (
            <svg width="16" height="16" fill="none" stroke="currentColor" viewBox="0 0 24 24" strokeWidth="2.5">
              <path strokeLinecap="round" strokeLinejoin="round" d="M14 5l7 7m0 0l-7 7m7-7H3" />
            </svg>
          )}
        </button>
      </div>
    </div>
  );
}
```

- [ ] **Step 2: Commit**

```powershell
git add frontend/src/components/DocumentChat.js
git commit -m "feat: add DocumentChat component for RAG Q&A"
```

---

## Task 7: Tích hợp DocumentChat vào trang chi tiết tài liệu

**Files:**
- Modify: `frontend/src/app/documents/[id]/page.js`

- [ ] **Step 1: Import DocumentChat**

Mở `frontend/src/app/documents/[id]/page.js`. Tìm dòng import cuối cùng ở đầu file (xung quanh dòng 11-12):
```javascript
import { getYoutubeId, getYoutubeEmbedUrl } from "@/lib/youtube";
```

Thêm ngay bên dưới:
```javascript
import DocumentChat from "@/components/DocumentChat";
```

- [ ] **Step 2: Nhúng DocumentChat vào left column**

Trong JSX, tìm đoạn kết thúc của left column. Tìm thẻ đóng `</div>` sau tab content:
```jsx
            </div>
          </div>

          {/* ── SIDEBAR ── */}
```

Thêm `<DocumentChat>` ngay trước comment `{/* ── SIDEBAR ── */}`:

```jsx
            </div>

            {/* RAG Chat */}
            <DocumentChat
              materialId={doc._id}
              hasContent={doc.hasContentText}
            />
          </div>

          {/* ── SIDEBAR ── */}
```

- [ ] **Step 3: Kiểm tra lại state `doc` đã có `hasContentText`**

Tìm dòng `setDoc(docData)` (khoảng dòng 100) trong `useEffect` fetch dữ liệu. Dữ liệu trả về từ `getMaterialById` đã có `hasContentText` sau Task 3 — không cần thay đổi gì thêm.

- [ ] **Step 4: Chạy frontend và test**

```powershell
cd frontend
npm run dev
```

1. Mở trình duyệt tại `http://localhost:3000`
2. Điều hướng đến trang chi tiết của một tài liệu PDF/DOCX có nội dung
3. Cuộn xuống dưới tab content — phải thấy panel "Hỏi AI về tài liệu này"
4. Gõ câu hỏi → nhấn Enter hoặc nút gửi
5. Kiểm tra AI trả lời đúng dựa trên nội dung tài liệu (không hallucinate kiến thức ngoài)

Test tài liệu không có contentText (video hoặc pptx cũ chưa re-embed):
6. Điều hướng đến trang chi tiết video hoặc tài liệu không có contentText
7. Panel chat phải **không xuất hiện** (component return null)

- [ ] **Step 5: Commit cuối**

```powershell
git add frontend/src/app/documents/[id]/page.js
git commit -m "feat: integrate DocumentChat into document detail page"
```

---

## Self-Review Checklist (đã thực hiện trước khi giao)

- [x] Spec coverage: tất cả 7 files trong spec đều có task tương ứng
- [x] PPTX extraction: đã verify là có trong extractText.js, task 1 chỉ cần install + re-embed
- [x] hasContentText: tính đúng (`!!(contentText && trim().length > 0)`), delete contentText trước khi trả về
- [x] chunkCache key: dùng `materialId.toString()` — đúng với cả ObjectId và string
- [x] Error statusCode: 404 (doc not found), 400 (no contentText), 500 (OpenAI error) — khớp với controller
- [x] Rate limit: dùng lại `aiLimiter` hiện có, không tạo mới
- [x] Frontend: component return null khi `!hasContent` — không render gì cả
- [x] Messages slice(-40): giới hạn 20 cặp Q&A trong UI
- [x] generateEmbedding đã được export từ aiService — chatWithDocument dùng được ngay
