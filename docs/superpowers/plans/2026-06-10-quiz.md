# Quiz Tự Động Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Thêm tính năng tạo quiz trắc nghiệm từ nội dung tài liệu, hiển thị modal trên trang chi tiết, chấm điểm client-side.

**Architecture:** gpt-4o-mini nhận toàn bộ `contentText` (giống chatWithDocument), sinh 5 câu trắc nghiệm JSON có cấu trúc. Frontend modal với 3 phase: loading → quiz → result. Session-only, không lưu DB.

**Tech Stack:** Node.js/Express, Next.js App Router, OpenAI gpt-4o-mini, MongoDB/Mongoose, Tailwind CSS

---

## File Map

| File | Vai trò |
|---|---|
| `Backend/services/aiService.js` | Thêm `generateQuiz` |
| `Backend/controllers/recommendationController.js` | Thêm `quizDocument` handler |
| `Backend/routes/recommendationRoutes.js` | Thêm `POST /quiz` |
| `frontend/src/components/DocumentQuiz.js` | Modal quiz component (mới) |
| `frontend/src/app/documents/[id]/page.js` | Thêm nút + state + import DocumentQuiz |

---

## Task 1: Thêm `generateQuiz` vào aiService.js

**Files:**
- Modify: `Backend/services/aiService.js`

- [ ] **Step 1: Thêm hàm `generateQuiz` trước `module.exports`**

Mở `Backend/services/aiService.js`. Tìm dòng cuối cùng:
```javascript
module.exports = { generateEmbedding, generateMetadata, expandQuery, buildEmbeddingText, chatWithDocument };
```

Thêm hàm `generateQuiz` ngay TRƯỚC dòng đó:

```javascript
const generateQuiz = async (materialId) => {
  const Material = require("../models/Material");

  const doc = await Material.findById(materialId).select("contentText");
  if (!doc) {
    const err = new Error("Tài liệu không tồn tại");
    err.statusCode = 404;
    throw err;
  }
  if (!doc.contentText || doc.contentText.trim().length === 0) {
    const err = new Error("Tài liệu này chưa hỗ trợ quiz");
    err.statusCode = 400;
    throw err;
  }

  const response = await openai.chat.completions.create({
    model: "gpt-4o-mini",
    messages: [
      {
        role: "user",
        content: `Dựa vào nội dung tài liệu dưới đây, hãy tạo đúng 5 câu hỏi trắc nghiệm để kiểm tra hiểu biết.

Yêu cầu:
- Mỗi câu có đúng 4 đáp án
- Chỉ 1 đáp án đúng
- Câu hỏi phải dựa trực tiếp vào nội dung, không bịa
- Đáp án nhiễu phải hợp lý, không quá dễ đoán
- Ngôn ngữ: tiếng Việt

Trả về JSON với format sau (không giải thích thêm):
{"questions":[{"question":"...","options":["...","...","...","..."],"answer":0}]}

"answer" là index (0-3) của đáp án đúng trong mảng options.

Nội dung tài liệu:
${doc.contentText}`,
      },
    ],
    response_format: { type: "json_object" },
    max_tokens: 1500,
    temperature: 0.5,
  });

  const parsed = JSON.parse(response.choices[0].message.content);
  if (!Array.isArray(parsed.questions) || parsed.questions.length === 0) {
    const err = new Error("Không thể tạo quiz từ tài liệu này");
    err.statusCode = 400;
    throw err;
  }
  return parsed.questions;
};
```

- [ ] **Step 2: Thêm `generateQuiz` vào `module.exports`**

Thay:
```javascript
module.exports = { generateEmbedding, generateMetadata, expandQuery, buildEmbeddingText, chatWithDocument };
```

Thành:
```javascript
module.exports = { generateEmbedding, generateMetadata, expandQuery, buildEmbeddingText, chatWithDocument, generateQuiz };
```

- [ ] **Step 3: Syntax check**

```powershell
node -e "require('./Backend/services/aiService.js')" 2>&1
```
Working dir: `D:\datahub\data_hub`

Expected: không có SyntaxError (lỗi runtime về thiếu OPENAI_API_KEY là bình thường).

- [ ] **Step 4: Commit**

```powershell
git -C D:\datahub\data_hub add Backend/services/aiService.js
git -C D:\datahub\data_hub commit -m "feat: add generateQuiz to aiService"
```

---

## Task 2: Thêm `quizDocument` controller

**Files:**
- Modify: `Backend/controllers/recommendationController.js`

- [ ] **Step 1: Thêm `generateQuiz` vào import ở đầu file**

Tìm dòng 2:
```javascript
const { chatWithDocument } = require("../services/aiService");
```

Thay thành:
```javascript
const { chatWithDocument, generateQuiz } = require("../services/aiService");
```

- [ ] **Step 2: Thêm handler `quizDocument` ở cuối file**

Thêm sau `exports.chatDocument`:

```javascript

// Tạo quiz trắc nghiệm từ nội dung tài liệu
exports.quizDocument = async (req, res) => {
  try {
    const { materialId } = req.body;

    if (!materialId) {
      return res.status(400).json({ success: false, message: "Thiếu materialId" });
    }
    if (!mongoose.Types.ObjectId.isValid(materialId)) {
      return res.status(400).json({ success: false, message: "materialId không hợp lệ" });
    }

    const questions = await generateQuiz(materialId);
    res.json({ success: true, questions });
  } catch (error) {
    const status = error.statusCode || 500;
    const message = status === 500 ? "AI đang bận, vui lòng thử lại sau" : error.message;
    res.status(status).json({ success: false, message });
  }
};
```

- [ ] **Step 3: Syntax check**

```powershell
node -e "require('./Backend/controllers/recommendationController.js')" 2>&1
```
Working dir: `D:\datahub\data_hub`

Expected: không có SyntaxError.

- [ ] **Step 4: Commit**

```powershell
git -C D:\datahub\data_hub add Backend/controllers/recommendationController.js
git -C D:\datahub\data_hub commit -m "feat: add quizDocument controller"
```

---

## Task 3: Thêm route `POST /quiz`

**Files:**
- Modify: `Backend/routes/recommendationRoutes.js`

- [ ] **Step 1: Thêm route**

Tìm dòng:
```javascript
router.post("/chat", aiLimiter, recommendationController.chatDocument);
```

Thêm ngay bên dưới:
```javascript
// Quiz tự động từ nội dung tài liệu (public, no auth required)
router.post("/quiz", aiLimiter, recommendationController.quizDocument);
```

- [ ] **Step 2: Verify**

Đọc lại file, xác nhận có đủ 5 routes:
- `GET /similar/:materialId`
- `GET /search`
- `GET /for-you`
- `POST /chat`
- `POST /quiz`

- [ ] **Step 3: Commit**

```powershell
git -C D:\datahub\data_hub add Backend/routes/recommendationRoutes.js
git -C D:\datahub\data_hub commit -m "feat: add POST /api/recommendations/quiz route"
```

---

## Task 4: Tạo `DocumentQuiz.js` component

**Files:**
- Create: `frontend/src/components/DocumentQuiz.js`

- [ ] **Step 1: Đọc AGENTS.md**

Đọc `D:\datahub\data_hub\frontend\AGENTS.md` trước khi viết code.

- [ ] **Step 2: Tạo component**

Tạo `D:\datahub\data_hub\frontend\src\components\DocumentQuiz.js`:

```javascript
"use client";
import { useState, useEffect } from "react";

export default function DocumentQuiz({ materialId, hasContent, isOpen, onClose }) {
  const [phase, setPhase] = useState("idle"); // "idle" | "loading" | "quiz" | "result" | "error"
  const [questions, setQuestions] = useState([]);
  const [selected, setSelected] = useState({}); // { [questionIndex]: optionIndex }
  const [errorMsg, setErrorMsg] = useState("");

  useEffect(() => {
    if (!isOpen) {
      setPhase("idle");
      setQuestions([]);
      setSelected({});
      setErrorMsg("");
      return;
    }
    fetchQuiz();
  }, [isOpen]);

  const fetchQuiz = async () => {
    setPhase("loading");
    setErrorMsg("");
    try {
      const res = await fetch(
        `${process.env.NEXT_PUBLIC_API_URL || "http://localhost:5000"}/api/recommendations/quiz`,
        {
          method: "POST",
          headers: { "Content-Type": "application/json" },
          body: JSON.stringify({ materialId }),
        }
      );
      const data = await res.json();
      if (!res.ok) {
        const err = new Error(data.message || "Lỗi không xác định");
        err.status = res.status;
        throw err;
      }
      setQuestions(data.questions);
      setPhase("quiz");
    } catch (err) {
      setErrorMsg(
        err.status === 429
          ? "Bạn tạo quiz quá nhanh, chờ chút nhé."
          : "AI đang bận, vui lòng thử lại sau."
      );
      setPhase("error");
    }
  };

  const score = questions.filter((q, i) => selected[i] === q.answer).length;
  const allAnswered = questions.length > 0 && Object.keys(selected).length === questions.length;

  if (!hasContent || !isOpen) return null;

  return (
    <div className="fixed inset-0 z-50 flex items-center justify-center p-4">
      {/* Backdrop */}
      <div className="absolute inset-0 bg-black/50 backdrop-blur-sm" onClick={onClose} />

      {/* Modal */}
      <div className="relative w-full max-w-2xl bg-white rounded-3xl shadow-2xl max-h-[85vh] overflow-y-auto">
        {/* Header */}
        <div className="sticky top-0 bg-white border-b border-slate-100 px-6 py-4 flex items-center justify-between rounded-t-3xl z-10">
          <div className="flex items-center gap-3">
            <div className="w-8 h-8 rounded-xl bg-violet-500 flex items-center justify-center shrink-0">
              <svg width="15" height="15" fill="none" stroke="white" viewBox="0 0 24 24" strokeWidth="2.5">
                <path strokeLinecap="round" strokeLinejoin="round" d="M9 12h3.75M9 15h3.75M9 18h3.75m3 .75H18a2.25 2.25 0 002.25-2.25V6.108c0-1.135-.845-2.098-1.976-2.192a48.424 48.424 0 00-1.123-.08m-5.801 0c-.065.21-.1.433-.1.664 0 .414.336.75.75.75h4.5a.75.75 0 00.75-.75 2.25 2.25 0 00-.1-.664m-5.8 0A2.251 2.251 0 0113.5 2.25H15c1.012 0 1.867.668 2.15 1.586m-5.8 0c-.376.023-.75.05-1.124.08C9.095 4.01 8.25 4.973 8.25 6.108V8.25m0 0H4.875c-.621 0-1.125.504-1.125 1.125v11.25c0 .621.504 1.125 1.125 1.125h9.75c.621 0 1.125-.504 1.125-1.125V9.375c0-.621-.504-1.125-1.125-1.125H8.25z" />
              </svg>
            </div>
            <div>
              <p className="text-sm font-black text-slate-800">Quiz kiểm tra kiến thức</p>
              <p className="text-[10px] font-bold text-slate-400 uppercase tracking-widest">
                {phase === "result" ? `Kết quả: ${score}/${questions.length}` : "5 câu trắc nghiệm"}
              </p>
            </div>
          </div>
          <button
            onClick={onClose}
            className="w-8 h-8 rounded-xl bg-slate-100 hover:bg-slate-200 flex items-center justify-center transition-colors"
          >
            <svg width="14" height="14" fill="none" stroke="#64748b" viewBox="0 0 24 24" strokeWidth="2.5">
              <path strokeLinecap="round" strokeLinejoin="round" d="M6 18L18 6M6 6l12 12" />
            </svg>
          </button>
        </div>

        <div className="p-6">
          {/* Loading */}
          {phase === "loading" && (
            <div className="flex flex-col items-center justify-center py-16 gap-4">
              <div className="w-10 h-10 border-4 border-violet-200 border-t-violet-500 rounded-full animate-spin" />
              <p className="text-sm font-medium text-slate-500">AI đang tạo câu hỏi từ tài liệu...</p>
            </div>
          )}

          {/* Error */}
          {phase === "error" && (
            <div className="flex flex-col items-center justify-center py-16 gap-4">
              <p className="text-sm font-medium text-red-500">{errorMsg}</p>
              <button
                onClick={fetchQuiz}
                className="px-5 py-2.5 bg-violet-500 text-white text-sm font-bold rounded-2xl hover:bg-violet-400 transition-colors"
              >
                Thử lại
              </button>
            </div>
          )}

          {/* Quiz */}
          {phase === "quiz" && (
            <div className="space-y-6">
              {questions.map((q, qi) => (
                <div key={qi} className="space-y-3">
                  <p className="text-sm font-bold text-slate-800">
                    <span className="text-violet-500 mr-1">Câu {qi + 1}.</span>
                    {q.question}
                  </p>
                  <div className="space-y-2">
                    {q.options.map((opt, oi) => (
                      <button
                        key={oi}
                        onClick={() => setSelected((prev) => ({ ...prev, [qi]: oi }))}
                        className={`w-full text-left px-4 py-3 rounded-2xl text-sm font-medium border transition-all ${
                          selected[qi] === oi
                            ? "bg-violet-500 text-white border-violet-500"
                            : "bg-slate-50 text-slate-700 border-slate-200 hover:border-violet-300 hover:bg-violet-50"
                        }`}
                      >
                        {opt}
                      </button>
                    ))}
                  </div>
                </div>
              ))}

              <button
                onClick={() => setPhase("result")}
                disabled={!allAnswered}
                className="w-full py-4 rounded-2xl bg-violet-500 hover:bg-violet-400 text-white text-sm font-black transition-all active:scale-95 disabled:opacity-40 disabled:cursor-not-allowed"
              >
                Nộp bài ({Object.keys(selected).length}/{questions.length})
              </button>
            </div>
          )}

          {/* Result */}
          {phase === "result" && (
            <div className="space-y-5">
              {/* Score banner */}
              <div className="text-center py-6 bg-violet-50 rounded-2xl">
                <p className="text-5xl font-black text-violet-500">{score}/{questions.length}</p>
                <p className="text-sm font-medium text-slate-500 mt-2">
                  {score === questions.length
                    ? "Xuất sắc! Bạn hiểu rất rõ tài liệu này."
                    : score >= 3
                    ? "Tốt! Bạn đã nắm được phần lớn nội dung."
                    : "Cần đọc lại tài liệu thêm nhé."}
                </p>
              </div>

              {/* Review từng câu */}
              {questions.map((q, qi) => {
                const isCorrect = selected[qi] === q.answer;
                return (
                  <div
                    key={qi}
                    className={`rounded-2xl border p-4 space-y-2 ${
                      isCorrect ? "bg-emerald-50 border-emerald-200" : "bg-red-50 border-red-200"
                    }`}
                  >
                    <p className="text-sm font-bold text-slate-800">
                      <span className={`mr-1.5 ${isCorrect ? "text-emerald-500" : "text-red-500"}`}>
                        {isCorrect ? "✓" : "✗"}
                      </span>
                      <span className="text-slate-400 mr-1">Câu {qi + 1}.</span>
                      {q.question}
                    </p>
                    <div className="space-y-1">
                      {q.options.map((opt, oi) => (
                        <p
                          key={oi}
                          className={`text-xs font-medium px-3 py-1.5 rounded-xl ${
                            oi === q.answer
                              ? "bg-emerald-500 text-white"
                              : oi === selected[qi] && !isCorrect
                              ? "bg-red-400 text-white"
                              : "text-slate-400"
                          }`}
                        >
                          {opt}
                        </p>
                      ))}
                    </div>
                  </div>
                );
              })}

              <button
                onClick={onClose}
                className="w-full py-4 rounded-2xl bg-slate-900 hover:bg-slate-700 text-white text-sm font-black transition-all active:scale-95"
              >
                Đóng
              </button>
            </div>
          )}
        </div>
      </div>
    </div>
  );
}
```

- [ ] **Step 3: Commit**

```powershell
git -C D:\datahub\data_hub add frontend/src/components/DocumentQuiz.js
git -C D:\datahub\data_hub commit -m "feat: add DocumentQuiz modal component"
```

---

## Task 5: Tích hợp vào trang chi tiết tài liệu

**Files:**
- Modify: `frontend/src/app/documents/[id]/page.js`

- [ ] **Step 1: Thêm import**

Tìm dòng 12:
```javascript
import DocumentChat from "@/components/DocumentChat";
```

Thêm ngay bên dưới:
```javascript
import DocumentQuiz from "@/components/DocumentQuiz";
```

- [ ] **Step 2: Thêm state `quizOpen`**

Tìm các `useState` khai báo ở đầu component (khoảng dòng 15-30). Thêm vào cùng nhóm:
```javascript
const [quizOpen, setQuizOpen] = useState(false);
```

- [ ] **Step 3: Thêm nút "Tạo Quiz" trong sidebar**

Tìm đoạn kết thúc của Download card (khoảng dòng 963-966):
```jsx
              </div>
            </div>

            {/* Related docs */}
```

Thêm nút giữa `</div>` (đóng download card) và `{/* Related docs */}`:
```jsx
              </div>
            </div>

            {/* Quiz button */}
            {doc?.hasContentText && (
              <button
                onClick={() => setQuizOpen(true)}
                className="w-full py-4 rounded-2xl bg-violet-500 hover:bg-violet-400 text-white text-sm font-black flex items-center justify-center gap-2.5 transition-all active:scale-95"
              >
                <svg width="16" height="16" fill="none" stroke="currentColor" viewBox="0 0 24 24" strokeWidth="2.5">
                  <path strokeLinecap="round" strokeLinejoin="round" d="M9 12h3.75M9 15h3.75M9 18h3.75m3 .75H18a2.25 2.25 0 002.25-2.25V6.108c0-1.135-.845-2.098-1.976-2.192a48.424 48.424 0 00-1.123-.08m-5.801 0c-.065.21-.1.433-.1.664 0 .414.336.75.75.75h4.5a.75.75 0 00.75-.75 2.25 2.25 0 00-.1-.664m-5.8 0A2.251 2.251 0 0113.5 2.25H15c1.012 0 1.867.668 2.15 1.586m-5.8 0c-.376.023-.75.05-1.124.08C9.095 4.01 8.25 4.973 8.25 6.108V8.25m0 0H4.875c-.621 0-1.125.504-1.125 1.125v11.25c0 .621.504 1.125 1.125 1.125h9.75c.621 0 1.125-.504 1.125-1.125V9.375c0-.621-.504-1.125-1.125-1.125H8.25z" />
                </svg>
                Tạo Quiz
              </button>
            )}

            {/* Related docs */}
```

- [ ] **Step 4: Render DocumentQuiz modal**

Tìm dòng 1039 (sau AddToCollectionModal):
```jsx
      <AddToCollectionModal isOpen={isCollectionModalOpen} onClose={() => setIsCollectionModalOpen(false)} materialId={params.id} />
```

Thêm ngay bên dưới:
```jsx
      <DocumentQuiz
        materialId={doc?._id}
        hasContent={doc?.hasContentText}
        isOpen={quizOpen}
        onClose={() => setQuizOpen(false)}
      />
```

- [ ] **Step 5: Kiểm tra không có lỗi contentText trong frontend**

```powershell
Select-String -Path "D:\datahub\data_hub\frontend\src" -Pattern "quizOpen" -Recurse -Include "*.js","*.jsx"
```

Expected: thấy kết quả trong `page.js` và `DocumentQuiz.js`.

- [ ] **Step 6: Commit**

```powershell
git -C D:\datahub\data_hub add "frontend/src/app/documents/[id]/page.js"
git -C D:\datahub\data_hub commit -m "feat: integrate DocumentQuiz into document detail page"
```

---

## Self-Review Checklist

- [x] Spec coverage: 5 files trong spec đều có task tương ứng
- [x] `generateQuiz` export khớp với import trong controller
- [x] `isOpen`/`onClose` props nhất quán từ page.js → DocumentQuiz
- [x] `useEffect` reset state khi `isOpen` → false
- [x] `allAnswered` guard trước khi submit
- [x] Error phase có nút "Thử lại" gọi lại `fetchQuiz`
- [x] Rate limit: dùng lại `aiLimiter`, không tạo mới
- [x] `hasContent` guard: nút quiz không hiện khi `!doc?.hasContentText`
